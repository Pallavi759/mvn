-name:Deploy artifact
 hosts:localhost
 become:true # If you need sudo/root acce3s
 tasks:
   -name: Ensure the deployment directory exists 
    file:
      path:/opt/deploy
      state:directory
      mode:'0755'
   -name:Cgpy the artifact to the deployment directory
    copy:
      src: /var/Lib/jenkins/workapace/mvn/target/HelloMaven-1.O-SNAPSHOT.jar   #Replace with actual file
      dest:/opt/deploy/ansi.jar
